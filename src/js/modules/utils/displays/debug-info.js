/**
 * DISPLAY DEBUG INFO
 * @module utils/displays/debug-info
 */

// Import components
import { cliDebugInfo } from "../../components/index.js";

/**
 * Display CLI Debug Info
 *
 * Prints the CLI debug information.
 */
export const displayDebugInfo = () => cliDebugInfo();
